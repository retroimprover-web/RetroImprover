# 🚀 Быстрый деплой RetroImprover

## ✅ Что уже готово

- ✅ Проект работает локально
- ✅ Переменные окружения настроены
- ✅ OAuth частично настроен (доделаем после деплоя)
- ✅ Домен уже есть

## 🎯 Рекомендуемый план: Railway (Backend) + Vercel (Frontend)

### Шаг 1: Подготовка GitHub репозитория

Если проект еще не в Git:

```bash
cd /Users/andrejursov/Documents/Work/RetroImprover
git init
git add .
git commit -m "Initial commit: RetroImprover ready for deployment"
# Создайте репозиторий на GitHub и выполните:
# git remote add origin https://github.com/yourusername/retroimprover.git
# git push -u origin main
```

### Шаг 2: Деплой Backend на Railway (10 минут)

1. **Зарегистрируйтесь на [Railway.app](https://railway.app)**
   - Войдите через GitHub

2. **Создайте новый проект:**
   - Нажмите **"New Project"**
   - Выберите **"Deploy from GitHub repo"**
   - Выберите ваш репозиторий `retroimprover`

3. **Настройте сервис:**
   - Railway автоматически определит, что это Node.js проект
   - **Root Directory**: `backend`
   - Railway автоматически запустит сборку

4. **Добавьте PostgreSQL:**
   - В проекте нажмите **"+ New"** → **"Database"** → **"Add PostgreSQL"**
   - Railway автоматически создаст базу данных
   - Скопируйте `DATABASE_URL` из переменных окружения базы данных

5. **Настройте переменные окружения:**
   - В настройках сервиса → **"Variables"**
   - Добавьте все переменные из `backend/.env.example`:
   
   ```env
   NODE_ENV=production
   PORT=3000
   DATABASE_URL=<скопируйте из PostgreSQL сервиса>
   JWT_SECRET=<сгенерируйте случайный ключ>
   GOOGLE_GENAI_API_KEY=<ваш ключ>
   FRONTEND_URL=https://your-frontend-domain.com (пока оставьте временный, обновим после деплоя фронтенда)
   UPLOAD_DIR=./uploads
   MAX_FILE_SIZE=10485760
   ```

6. **Настройте миграции:**
   - Railway автоматически запустит `npm install` и `npm run build`
   - Нужно добавить команду для миграций:
   - В настройках → **"Deploy"** → **"Deploy Command"**:
   ```
   npm install && npx prisma generate && npx prisma migrate deploy && npm run build
   ```

7. **Получите URL бэкенда:**
   - После деплоя Railway даст URL типа: `https://retroimprover-backend-production.up.railway.app`
   - Скопируйте этот URL

### Шаг 3: Деплой Frontend на Vercel (5 минут)

1. **Зарегистрируйтесь на [Vercel.com](https://vercel.com)**
   - Войдите через GitHub

2. **Импортируйте проект:**
   - Нажмите **"Add New..."** → **"Project"**
   - Выберите ваш репозиторий `retroimprover`
   - Vercel автоматически определит, что это Vite проект

3. **Настройте проект:**
   - **Framework Preset**: Vite
   - **Root Directory**: `.` (корень)
   - **Build Command**: `npm run build` (по умолчанию)
   - **Output Directory**: `dist` (по умолчанию)

4. **Добавьте переменные окружения:**
   - В настройках проекта → **"Environment Variables"**
   - Добавьте:
   ```
   VITE_API_URL=https://your-backend-railway-url.up.railway.app
   ```
   (используйте URL бэкенда из Railway)

5. **Деплой:**
   - Нажмите **"Deploy"**
   - Vercel соберет и задеплоит проект
   - Получите URL фронтенда типа: `https://retroimprover.vercel.app`

### Шаг 4: Обновить переменные окружения

1. **В Railway (Backend):**
   - Обновите `FRONTEND_URL` на URL из Vercel:
   ```
   FRONTEND_URL=https://retroimprover.vercel.app
   ```

2. **Перезапустите backend:**
   - В Railway нажмите **"Redeploy"**

### Шаг 5: Настроить домен

#### На Vercel (Frontend):

1. В настройках проекта → **"Domains"**
2. Добавьте ваш домен (например: `retroimprover.com`)
3. Vercel покажет DNS записи для настройки
4. Настройте DNS у вашего регистратора домена:
   - **CNAME**: `www` → `cname.vercel-dns.com`
   - **A record**: `@` → IP адрес (если нужен корневой домен)

#### На Railway (Backend):

1. В настройках сервиса → **"Settings"** → **"Networking"**
2. Нажмите **"Generate Domain"** или добавьте кастомный домен
3. Если нужен кастомный домен:
   - Добавьте домен (например: `api.retroimprover.com`)
   - Настройте DNS:
     - **CNAME**: `api` → `railway.app` (или указанный Railway URL)

### Шаг 6: Обновить OAuth настройки

После того как домены настроены:

1. **В Google Cloud Console:**
   - Обновите **Authorized JavaScript origins**:
     ```
     https://your-backend-domain.com
     ```
   - Обновите **Authorized redirect URIs**:
     ```
     https://your-backend-domain.com/api/auth/google/callback
     ```

2. **В Railway (Backend):**
   - Обновите переменные окружения:
   ```
   GOOGLE_CALLBACK_URL=https://your-backend-domain.com/api/auth/google/callback
   ```

3. **Перезапустите backend**

### Шаг 7: Проверка

1. ✅ Откройте фронтенд на вашем домене
2. ✅ Проверьте `/health` endpoint бэкенда
3. ✅ Попробуйте зарегистрироваться
4. ✅ Попробуйте загрузить и восстановить фото
5. ✅ Проверьте OAuth (после настройки)

## 🔧 Альтернатива: Render (все в одном месте)

Если хотите использовать Render вместо Railway+Vercel:

### Backend на Render:

1. Создайте **Web Service**
2. Подключите GitHub репозиторий
3. **Root Directory**: `backend`
4. **Build Command**: `npm install && npx prisma generate && npm run build`
5. **Start Command**: `npx prisma migrate deploy && npm start`
6. Добавьте PostgreSQL базу данных
7. Настройте переменные окружения

### Frontend на Render:

1. Создайте **Static Site**
2. Подключите GitHub репозиторий
3. **Build Command**: `npm install && npm run build`
4. **Publish Directory**: `dist`
5. Настройте переменные окружения

## ⚠️ Важные замечания

### Файлы (uploads)

В продакшене локальная папка `uploads` не подходит. Нужно:
- Использовать S3 (AWS) или другой cloud storage
- Или использовать Railway/Render volumes (платно)

**Временное решение для MVP:**
- Оставить локальную папку (файлы будут теряться при перезапуске)
- Позже мигрировать на S3

### База данных

- Railway: PostgreSQL создается автоматически
- Render: Нужно создать отдельный PostgreSQL сервис
- Миграции запускаются автоматически при деплое

### Безопасность

- ✅ Никогда не коммитьте `.env` файлы
- ✅ Используйте сильные `JWT_SECRET`
- ✅ Настройте CORS правильно (`FRONTEND_URL`)

## 📞 Если что-то пошло не так

1. **Проверьте логи:**
   - Railway: Вкладка "Deployments" → выберите деплой → "View Logs"
   - Vercel: Вкладка "Deployments" → выберите деплой → "View Function Logs"

2. **Проверьте переменные окружения:**
   - Убедитесь, что все переменные заданы правильно

3. **Проверьте базу данных:**
   - Убедитесь, что миграции применились
   - Проверьте `DATABASE_URL`

## ✅ Готово!

После выполнения всех шагов ваш сайт будет доступен в интернете на вашем домене!

