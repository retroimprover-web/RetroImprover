# Исправление генерации видео (Veo API)

## Проблемы, которые были исправлены:

1. ✅ **Убран хардкод API ключа** - теперь используется только переменная окружения
2. ✅ **Добавлена проверка наличия API ключа** при запуске
3. ✅ **Улучшено логирование** для отладки проблем
4. ✅ **Улучшена обработка ошибок** с понятными сообщениями

## Проверка API ключа в Railway

### Шаг 1: Проверьте переменную окружения

1. Откройте ваш проект на Railway: https://railway.app/
2. Выберите ваш backend сервис
3. Перейдите в раздел **"Variables"**
4. Найдите переменную `GOOGLE_GENAI_API_KEY`
5. Убедитесь, что:
   - ✅ Переменная существует
   - ✅ Значение установлено (не пустое)
   - ✅ Ключ начинается с `AIzaSy` (стандартный формат Google API ключей)

### Шаг 2: Получите новый API ключ (если нужно)

Если ключ отсутствует или был скомпрометирован:

1. Перейдите в Google AI Studio: https://aistudio.google.com/
2. Войдите в свой Google аккаунт
3. Нажмите на иконку настроек (⚙️) или перейдите: https://aistudio.google.com/app/apikey
4. Нажмите **"Create API Key"**
5. Выберите проект (или создайте новый)
6. Скопируйте созданный API ключ

### Шаг 3: Обновите ключ в Railway

1. В Railway, в разделе **"Variables"**
2. Найдите или создайте переменную `GOOGLE_GENAI_API_KEY`
3. Вставьте новый API ключ
4. Сохраните изменения
5. Railway автоматически перезапустит сервис

### Шаг 4: Проверьте доступность Veo API

Veo API может быть доступен не для всех API ключей. Проверьте:

1. В Google AI Studio перейдите в раздел **"Models"**
2. Проверьте, доступна ли модель `veo-3.1-fast-generate-preview`
3. Если модель недоступна, возможно нужен доступ к preview/beta функциям

## Проверка логов

После обновления API ключа проверьте логи Railway:

1. В Railway откройте ваш backend сервис
2. Перейдите в раздел **"Deployments"** → выберите последний деплой
3. Откройте **"Logs"**
4. Попробуйте сгенерировать видео
5. Ищите в логах:
   - `📋 API Key установлен: ✅ Да` - ключ найден
   - `❌ GOOGLE_GENAI_API_KEY не установлен` - ключ отсутствует
   - `❌ Ошибка при генерации видео` - детали ошибки

## Типичные ошибки и решения

### Ошибка: "API key not valid"
**Решение:** Обновите `GOOGLE_GENAI_API_KEY` в Railway на новый ключ из Google AI Studio

### Ошибка: "403 Forbidden - Your API key was reported as leaked"
**Решение:** Ключ был скомпрометирован. Создайте новый ключ и обновите в Railway

### Ошибка: "404 Not Found - models/veo-3.1-fast-generate-preview is not found"
**Решение:** 
- Модель может быть недоступна для вашего API ключа
- Проверьте доступность Veo API в Google AI Studio
- Возможно нужен доступ к beta/preview функциям

### Ошибка: "401 Unauthorized"
**Решение:** 
- Проверьте, что `GOOGLE_GENAI_API_KEY` правильно установлен в Railway
- Убедитесь, что ключ не содержит лишних пробелов
- Перезапустите сервис после обновления ключа

## Структура запроса к Veo API

Код использует следующий endpoint:
```
POST https://generativelanguage.googleapis.com/v1beta/models/veo-3.1-fast-generate-preview:generateVideo?key={API_KEY}
```

Тело запроса:
```json
{
  "prompt": "Cinematic animation: ...",
  "image": {
    "imageBytes": "base64_encoded_image",
    "mimeType": "image/jpeg"
  },
  "config": {
    "aspectRatio": "3:4",
    "resolution": "720p"
  }
}
```

## Дополнительная информация

- Документация Google Veo API: https://ai.google.dev/docs
- Google AI Studio: https://aistudio.google.com/
- Railway Variables: https://docs.railway.app/develop/variables

