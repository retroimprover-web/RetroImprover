# Как запустить RetroImprover

## Проблема была в том, что:
1. ❌ Фронтенд пытался запуститься на порту 3000 (конфликт с бэкендом)
2. ❌ Не было package.json для фронтенда
3. ❌ Фронтенд не был запущен

## ✅ Что исправлено:
1. Порт фронтенда изменен на **5173** в `vite.config.ts`
2. Создан `package.json` для фронтенда
3. Теперь можно запустить фронтенд

## 🚀 Инструкция по запуску:

### Шаг 1: Запустите Backend (в одном терминале)

```bash
cd /Users/andrejursov/Documents/Work/RetroImprover/backend
npm run dev
```

Backend должен запуститься на `http://localhost:3000`

### Шаг 2: Установите зависимости фронтенда (если еще не установлены)

```bash
cd /Users/andrejursov/Documents/Work/RetroImprover
npm install
```

### Шаг 3: Запустите Frontend (в другом терминале)

```bash
cd /Users/andrejursov/Documents/Work/RetroImprover
npm run dev
```

Frontend должен запуститься на `http://localhost:5173`

## 🌐 Откройте в браузере:

**Frontend:** http://localhost:5173

**Backend API:** http://localhost:3000 (показывает JSON с информацией об API)

## ⚠️ Важно:

- Backend должен быть запущен **ПЕРЕД** тем, как вы будете использовать фронтенд
- Оба сервера должны работать одновременно
- Если видите ошибки CORS, проверьте, что `FRONTEND_URL=http://localhost:5173` в `.env` бэкенда

## 🔍 Проверка:

1. Откройте `http://localhost:3000/` - должен показаться JSON
2. Откройте `http://localhost:5173` - должен открыться сайт RetroImprover
3. Попробуйте зарегистрироваться или войти

## 📝 Если что-то не работает:

1. Проверьте, что оба сервера запущены:
   ```bash
   lsof -ti:3000 && echo "Backend работает" || echo "Backend НЕ работает"
   lsof -ti:5173 && echo "Frontend работает" || echo "Frontend НЕ работает"
   ```

2. Проверьте консоль браузера (F12) на наличие ошибок

3. Проверьте терминалы, где запущены серверы - там могут быть ошибки

