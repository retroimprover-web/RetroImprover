# 🚀 План деплоя RetroImprover

## Текущее состояние

✅ **Локальная разработка работает:**
- Backend: `http://localhost:3000`
- Frontend: `http://localhost:5173`
- База данных: PostgreSQL (локально)

## Шаги для деплоя в интернет

### Этап 1: Подготовка к деплою ✅

1. ✅ Настроены переменные окружения для API URL
2. ✅ API URL теперь конфигурируется через `VITE_API_URL`
3. ⏳ Проверка работоспособности локально

### Этап 2: Выбор платформы для деплоя

**Варианты:**

#### Вариант A: Vercel (Frontend) + Railway/Render (Backend) - Рекомендуется
- **Frontend на Vercel**: Бесплатно, быстрый CDN, автоматический деплой из Git
- **Backend на Railway/Render**: Простой деплой, встроенная PostgreSQL
- **Плюсы**: Просто, бесплатно для начала, хорошая производительность
- **Минусы**: Нужны два сервиса

#### Вариант B: Render (Full Stack)
- **Frontend + Backend на Render**: Один сервис для всего
- **Плюсы**: Все в одном месте
- **Минусы**: Медленнее чем Vercel для фронтенда

#### Вариант C: Собственный VPS (DigitalOcean, AWS, etc.)
- **Полный контроль**: Собственный сервер
- **Плюсы**: Полный контроль, можно настроить как угодно
- **Минусы**: Требует больше знаний, нужно настраивать самому

### Этап 3: Настройка базы данных

1. **Создать PostgreSQL базу данных:**
   - Railway: Автоматически при создании сервиса
   - Render: Отдельный PostgreSQL сервис
   - VPS: Установить PostgreSQL вручную

2. **Получить DATABASE_URL:**
   ```
   postgresql://user:password@host:5432/dbname
   ```

### Этап 4: Настройка переменных окружения

#### Backend (.env на сервере):
```env
NODE_ENV=production
PORT=3000
DATABASE_URL=postgresql://... (от вашего провайдера БД)
JWT_SECRET=your-super-secret-key-change-this
GOOGLE_GENAI_API_KEY=your-google-api-key
FRONTEND_URL=https://your-frontend-domain.com
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760

# OAuth (опционально)
GOOGLE_CLIENT_ID=...
GOOGLE_CLIENT_SECRET=...
GOOGLE_CALLBACK_URL=https://your-backend-domain.com/api/auth/google/callback
```

#### Frontend (.env на Vercel/Render):
```env
VITE_API_URL=https://your-backend-domain.com
```

### Этап 5: Деплой Backend

#### На Railway:
1. Подключить GitHub репозиторий
2. Выбрать папку `backend`
3. Указать переменные окружения
4. Railway автоматически создаст PostgreSQL
5. Запустит `npm run build && npm start`

#### На Render:
1. Создать новый Web Service
2. Подключить GitHub репозиторий
3. Root Directory: `backend`
4. Build Command: `npm install && npm run build`
5. Start Command: `npm start`
6. Добавить PostgreSQL базу данных
7. Указать переменные окружения

### Этап 6: Деплой Frontend

#### На Vercel:
1. Подключить GitHub репозиторий
2. Root Directory: `.` (корень проекта)
3. Build Command: `npm run build`
4. Output Directory: `dist`
5. Environment Variables: `VITE_API_URL=https://your-backend-domain.com`

#### На Render:
1. Создать Static Site
2. Подключить GitHub репозиторий
3. Build Command: `npm run build`
4. Publish Directory: `dist`

### Этап 7: Настройка домена

1. **Купить домен** (например, на Namecheap, GoDaddy, или через Vercel)
2. **Настроить DNS записи:**
   - Frontend: CNAME → Vercel/Render URL
   - Backend: A record или CNAME → Backend IP/URL
3. **Обновить переменные окружения:**
   - `FRONTEND_URL` в backend → ваш домен фронтенда
   - `VITE_API_URL` во frontend → ваш домен бэкенда
   - OAuth callback URLs → обновить на домены

### Этап 8: Проверка после деплоя

1. ✅ Frontend открывается на домене
2. ✅ Backend API отвечает на `/health`
3. ✅ Регистрация/вход работают
4. ✅ Загрузка и восстановление фото работают
5. ✅ База данных сохраняет данные
6. ✅ OAuth (если настроен) работает

## Быстрый старт (рекомендуемый путь)

### 1. Railway для Backend (5 минут)
```bash
# 1. Зарегистрироваться на railway.app
# 2. New Project → Deploy from GitHub
# 3. Выбрать репозиторий, папку backend
# 4. Railway автоматически:
#    - Установит зависимости
#    - Создаст PostgreSQL
#    - Запустит сервер
# 5. Получить URL бэкенда (например: https://retroimprover-backend.railway.app)
```

### 2. Vercel для Frontend (5 минут)
```bash
# 1. Зарегистрироваться на vercel.com
# 2. Import Project → GitHub
# 3. Root Directory: . (корень)
# 4. Environment Variables:
#    VITE_API_URL=https://retroimprover-backend.railway.app
# 5. Deploy
# 6. Получить URL фронтенда (например: https://retroimprover.vercel.app)
```

### 3. Обновить Backend переменные
```env
FRONTEND_URL=https://retroimprover.vercel.app
```

### 4. Настроить домен (опционально)
- В Vercel: Settings → Domains → добавить свой домен
- В Railway: Settings → Domains → добавить свой домен

## Важные замечания

⚠️ **Безопасность:**
- Никогда не коммитьте `.env` файлы в Git
- Используйте сильные `JWT_SECRET`
- Настройте CORS правильно

⚠️ **Файлы:**
- В продакшене используйте S3 или другой cloud storage для uploads
- Локальная папка `uploads` не подходит для продакшена

⚠️ **Производительность:**
- Включите кеширование статических файлов
- Настройте CDN для изображений
- Используйте connection pooling для БД

## Следующие шаги

1. Выбрать платформу для деплоя
2. Задеплоить backend
3. Задеплоить frontend
4. Протестировать все функции
5. Настроить домен
6. Настроить мониторинг и логирование

