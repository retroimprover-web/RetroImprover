# 🚀 Пошаговый деплой RetroImprover

## ✅ План: Railway (Backend) + Vercel (Frontend)

---

## 📦 Шаг 1: Подготовка Git репозитория

### 1.1. Инициализация Git (если еще не сделано)

```bash
cd /Users/andrejursov/Documents/Work/RetroImprover
git init
git add .
git commit -m "Initial commit: RetroImprover ready for deployment"
```

### 1.2. Создание репозитория на GitHub

1. Зайдите на [github.com](https://github.com)
2. Нажмите **"+"** → **"New repository"**
3. Название: `retroimprover` (или любое другое)
4. **Не** добавляйте README, .gitignore, license (у нас уже есть)
5. Нажмите **"Create repository"**

### 1.3. Подключение к GitHub

```bash
# Замените YOUR_USERNAME на ваш GitHub username
git remote add origin https://github.com/YOUR_USERNAME/retroimprover.git
git branch -M main
git push -u origin main
```

**Готово!** Код теперь на GitHub.

---

## 🚂 Шаг 2: Деплой Backend на Railway

### 2.1. Регистрация на Railway

1. Зайдите на [railway.app](https://railway.app)
2. Нажмите **"Login"** → **"Login with GitHub"**
3. Разрешите доступ Railway к вашему GitHub

### 2.2. Создание проекта

1. Нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Выберите репозиторий `retroimprover`
4. Railway начнет автоматический деплой

### 2.3. Настройка сервиса

1. В проекте Railway нажмите на сервис (или **"+ New"** → **"GitHub Repo"**)
2. Выберите репозиторий `retroimprover`
3. В настройках сервиса:
   - **Root Directory**: `backend`
   - Railway автоматически определит Node.js

### 2.4. Добавление PostgreSQL базы данных

1. В проекте Railway нажмите **"+ New"**
2. Выберите **"Database"** → **"Add PostgreSQL"**
3. Railway автоматически создаст базу данных
4. Скопируйте `DATABASE_URL` из переменных окружения базы данных

### 2.5. Настройка переменных окружения

В настройках backend сервиса → **"Variables"** → добавьте:

```env
NODE_ENV=production
PORT=3000
DATABASE_URL=<скопируйте из PostgreSQL сервиса>
JWT_SECRET=<сгенерируйте случайный ключ, например: openssl rand -base64 32>
GOOGLE_GENAI_API_KEY=<ваш ключ из .env>
FRONTEND_URL=https://your-frontend.vercel.app
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760
```

**Важно:** `FRONTEND_URL` пока оставьте временный, обновим после деплоя фронтенда.

### 2.6. Настройка команд деплоя

В настройках сервиса → **"Settings"** → **"Deploy"**:

- **Deploy Command**: 
  ```
  npm install && npx prisma generate && npm run build
  ```

- **Start Command**: 
  ```
  npx prisma migrate deploy && npm start
  ```

### 2.7. Получение URL бэкенда

1. После деплоя Railway даст URL типа: `https://xxx.up.railway.app`
2. Скопируйте этот URL — он понадобится для фронтенда
3. Проверьте работу: откройте `https://xxx.up.railway.app/health`

**Готово!** Backend работает.

---

## ⚡ Шаг 3: Деплой Frontend на Vercel

### 3.1. Регистрация на Vercel

1. Зайдите на [vercel.com](https://vercel.com)
2. Нажмите **"Sign Up"** → **"Continue with GitHub"**
3. Разрешите доступ Vercel к вашему GitHub

### 3.2. Импорт проекта

1. Нажмите **"Add New..."** → **"Project"**
2. Выберите репозиторий `retroimprover`
3. Vercel автоматически определит, что это Vite проект

### 3.3. Настройка проекта

В настройках проекта:

- **Framework Preset**: `Vite` (автоматически)
- **Root Directory**: `.` (корень проекта)
- **Build Command**: `npm run build` (по умолчанию)
- **Output Directory**: `dist` (по умолчанию)

### 3.4. Добавление переменных окружения

В настройках проекта → **"Environment Variables"** → добавьте:

```
VITE_API_URL=https://xxx.up.railway.app
```

(Замените на URL вашего бэкенда из Railway)

### 3.5. Деплой

1. Нажмите **"Deploy"**
2. Vercel соберет и задеплоит проект
3. После завершения получите URL типа: `https://retroimprover.vercel.app`

**Готово!** Frontend работает.

---

## 🔄 Шаг 4: Обновление переменных окружения

### 4.1. Обновить FRONTEND_URL в Railway

1. Зайдите в Railway → ваш backend сервис
2. **Variables** → найдите `FRONTEND_URL`
3. Обновите на URL из Vercel: `https://retroimprover.vercel.app`
4. Нажмите **"Redeploy"** в Railway

### 4.2. Проверка

1. Откройте фронтенд: `https://retroimprover.vercel.app`
2. Попробуйте зарегистрироваться
3. Проверьте, что все работает

---

## 🌐 Шаг 5: Настройка домена (опционально)

### 5.1. Frontend на Vercel

1. В Vercel → ваш проект → **"Settings"** → **"Domains"**
2. Нажмите **"Add"** → введите ваш домен (например: `retroimprover.com`)
3. Vercel покажет DNS записи для настройки
4. Настройте DNS у вашего регистратора домена:
   - **CNAME**: `www` → `cname.vercel-dns.com`
   - Или **A record** для корневого домена

### 5.2. Backend на Railway

1. В Railway → ваш сервис → **"Settings"** → **"Networking"**
2. Нажмите **"Generate Domain"** (или добавьте кастомный)
3. Если нужен кастомный домен (например: `api.retroimprover.com`):
   - Добавьте домен
   - Настройте DNS: **CNAME** → `railway.app`

### 5.3. Обновление переменных окружения

После настройки доменов обновите:

**В Railway:**
```
FRONTEND_URL=https://your-frontend-domain.com
```

**В Vercel:**
```
VITE_API_URL=https://your-backend-domain.com
```

**В Google OAuth Console:**
- Обновите Authorized JavaScript origins
- Обновите Authorized redirect URIs

---

## ✅ Проверка после деплоя

- [ ] Frontend открывается на домене
- [ ] Backend API отвечает на `/health`
- [ ] Регистрация работает
- [ ] Вход работает
- [ ] Загрузка фото работает
- [ ] Восстановление фото работает
- [ ] База данных сохраняет данные
- [ ] OAuth работает (после настройки)

---

## 🔧 Решение проблем

### Backend не запускается
- Проверьте логи в Railway
- Убедитесь, что `DATABASE_URL` правильный
- Проверьте, что миграции применились

### Frontend не подключается к Backend
- Проверьте `VITE_API_URL` в Vercel
- Проверьте CORS в настройках Railway (`FRONTEND_URL`)
- Откройте консоль браузера (F12) и проверьте ошибки

### Ошибки базы данных
- Проверьте, что PostgreSQL создан
- Проверьте `DATABASE_URL`
- Проверьте логи миграций в Railway

---

## 📝 Следующие шаги

1. ✅ Деплой завершен
2. ⏳ Настроить OAuth с правильными URL
3. ⏳ Настроить домен
4. ⏳ Настроить мониторинг (опционально)

**Готово! Ваш сайт работает в интернете! 🎉**

