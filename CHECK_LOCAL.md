# ✅ Проверка работоспособности локально

## Быстрая проверка

### 1. Запустить Backend

```bash
cd /Users/andrejursov/Documents/Work/RetroImprover/backend
npm run dev
```

**Ожидаемый результат:**
```
🚀 Сервер запущен на порту 3000
📁 Загрузки сохраняются в: /path/to/uploads
🌐 Frontend URL: http://localhost:5173
```

**Проверка:**
- Откройте `http://localhost:3000/` - должен показаться JSON с информацией об API
- Откройте `http://localhost:3000/health` - должен вернуться `{"status":"ok",...}`

### 2. Запустить Frontend (в другом терминале)

```bash
cd /Users/andrejursov/Documents/Work/RetroImprover
npm run dev
```

**Ожидаемый результат:**
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network:  http://192.168.x.x:5173/
```

### 3. Проверить в браузере

1. Откройте `http://localhost:5173`
2. Должен открыться экран авторизации RetroImprover
3. Попробуйте зарегистрироваться:
   - Email: `test@example.com`
   - Password: `test123`
4. После регистрации должен открыться главный экран
5. Попробуйте загрузить фото:
   - Нажмите "Tap to Upload"
   - Выберите изображение
   - Обрежьте фото
   - Нажмите "Restore (1 Star)"

### 4. Проверить консоль браузера

Откройте DevTools (F12) → Console
- Не должно быть ошибок CORS
- Не должно быть ошибок подключения к API

### 5. Проверить Network запросы

Откройте DevTools (F12) → Network
- При регистрации/входе должен быть запрос к `/api/auth/register` или `/api/auth/login`
- Статус должен быть `200 OK`

## Возможные проблемы

### Backend не запускается
- ✅ Проверьте, что PostgreSQL запущен
- ✅ Проверьте `DATABASE_URL` в `.env`
- ✅ Убедитесь, что порт 3000 свободен

### Frontend не подключается к Backend
- ✅ Проверьте, что backend запущен на порту 3000
- ✅ Проверьте CORS в консоли браузера
- ✅ Убедитесь, что `FRONTEND_URL=http://localhost:5173` в `.env` бэкенда

### Ошибки аутентификации
- ✅ Проверьте, что `JWT_SECRET` задан в `.env`
- ✅ Проверьте, что токен сохраняется в localStorage

## Если все работает ✅

Можно переходить к деплою! См. `DEPLOYMENT.md`

