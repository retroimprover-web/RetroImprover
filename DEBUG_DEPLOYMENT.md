# 🔍 Отладка проблем после деплоя

## Проблема: Регистрация не работает

### Шаг 1: Проверьте переменные окружения

#### В Vercel (Frontend):
1. Откройте проект → **Settings** → **Environment Variables**
2. Убедитесь, что есть:
   ```
   VITE_API_URL=https://your-backend-url.up.railway.app
   ```
   **Важно:** Без `/api` в конце! Код добавит его сам.

#### В Railway (Backend):
1. Откройте сервис → **Variables**
2. Убедитесь, что есть:
   ```
   FRONTEND_URL=https://your-frontend-url.vercel.app
   ```
   **Важно:** URL должен точно совпадать с вашим фронтендом!

### Шаг 2: Проверьте консоль браузера

1. Откройте сайт
2. Нажмите **F12** (или Cmd+Option+I на Mac)
3. Перейдите на вкладку **Console**
4. Попробуйте зарегистрироваться
5. Посмотрите на ошибки

**Возможные ошибки:**

#### CORS Error:
```
Access to fetch at 'https://...' from origin 'https://...' has been blocked by CORS policy
```
**Решение:** Проверьте `FRONTEND_URL` в Railway - должен точно совпадать с URL фронтенда

#### Network Error / Failed to fetch:
```
Network error: Could not connect to server
```
**Решение:** 
- Проверьте `VITE_API_URL` в Vercel
- Убедитесь, что бэкенд работает (откройте `https://your-backend-url.up.railway.app/health`)

#### 404 Not Found:
```
POST https://.../api/auth/register 404
```
**Решение:** Проверьте, что в `VITE_API_URL` нет `/api` в конце

### Шаг 3: Проверьте бэкенд

Откройте в браузере:
```
https://your-backend-url.up.railway.app/health
```

Должно вернуть:
```json
{"status":"ok","timestamp":"..."}
```

Если не работает:
- Проверьте логи в Railway
- Убедитесь, что деплой прошел успешно

### Шаг 4: Проверьте Network запросы

1. Откройте DevTools (F12)
2. Перейдите на вкладку **Network**
3. Попробуйте зарегистрироваться
4. Найдите запрос к `/api/auth/register`
5. Посмотрите:
   - **Status**: должен быть 200 или 201
   - **Request URL**: правильный ли URL?
   - **Response**: что возвращает сервер?

### Шаг 5: Проверьте логи

#### В Railway:
1. Откройте сервис → **Deployments**
2. Выберите последний деплой → **View Logs**
3. Ищите ошибки

#### В Vercel:
1. Откройте проект → **Deployments**
2. Выберите последний деплой → **View Function Logs**
3. Ищите ошибки

## Быстрое решение

### Если регистрация не работает:

1. **Проверьте VITE_API_URL в Vercel:**
   - Должен быть: `https://your-backend.up.railway.app`
   - Без `/api` в конце!

2. **Проверьте FRONTEND_URL в Railway:**
   - Должен быть: `https://your-frontend.vercel.app`
   - Точное совпадение с URL фронтенда!

3. **Перезапустите оба сервиса:**
   - В Railway: **Redeploy**
   - В Vercel: **Redeploy**

4. **Очистите кеш браузера:**
   - Ctrl+Shift+R (Windows/Linux)
   - Cmd+Shift+R (Mac)

## Проверка API URL

В консоли браузера (F12) должно быть:
```
API URL: https://your-backend.up.railway.app/api
```

Если видите `http://localhost:3000/api` - значит `VITE_API_URL` не настроен в Vercel!

