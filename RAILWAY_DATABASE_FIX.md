# 🔧 Исправление ошибки DATABASE_URL в Railway

## Проблема
```
Error: Environment variable not found: DATABASE_URL
```

## Решение

### Вариант 1: Автоматическая связь (Рекомендуется)

Railway автоматически связывает переменные из PostgreSQL сервиса, но нужно проверить:

1. **В Railway проекте:**
   - Убедитесь, что PostgreSQL сервис создан
   - Убедитесь, что backend сервис и PostgreSQL сервис находятся в **одном проекте**

2. **Проверьте переменные окружения:**
   - Откройте ваш **backend сервис** в Railway
   - Перейдите в **"Variables"**
   - Railway должен автоматически показать `DATABASE_URL` из PostgreSQL
   - Если его нет, нажмите **"Reference Variable"** или **"Add from Service"**
   - Выберите PostgreSQL сервис → `DATABASE_URL`

### Вариант 2: Ручное добавление

Если автоматическая связь не работает:

1. **Скопируйте DATABASE_URL из PostgreSQL:**
   - Откройте **PostgreSQL сервис** в Railway
   - Перейдите в **"Variables"**
   - Найдите `DATABASE_URL`
   - Скопируйте значение (оно выглядит как: `postgresql://user:password@host:port/dbname`)

2. **Добавьте в backend сервис:**
   - Откройте **backend сервис** → **"Variables"**
   - Нажмите **"+ New Variable"**
   - **Name**: `DATABASE_URL`
   - **Value**: вставьте скопированное значение
   - Нажмите **"Add"**

### Вариант 3: Использование Service Reference (Лучший способ)

Railway поддерживает ссылки на переменные других сервисов:

1. В **backend сервисе** → **"Variables"**
2. Нажмите **"Reference Variable"** или **"Add from Service"**
3. Выберите ваш **PostgreSQL сервис**
4. Выберите переменную `DATABASE_URL`
5. Railway автоматически создаст ссылку

Это лучше, потому что если PostgreSQL перезапустится и URL изменится, ссылка обновится автоматически.

## Проверка

После добавления `DATABASE_URL`:

1. Railway автоматически перезапустит деплой
2. Проверьте логи деплоя
3. Ошибка `Environment variable not found: DATABASE_URL` должна исчезнуть

## Если проблема остается

1. **Проверьте, что переменные применены:**
   - В логах деплоя должны быть видны переменные окружения
   - Railway показывает, какие переменные используются

2. **Проверьте порядок команд:**
   - В настройках деплоя команды должны быть:
     - **Deploy Command**: `npm install && npx prisma generate && npm run build`
     - **Start Command**: `npx prisma migrate deploy && npm start`
   
   Prisma generate должен выполняться **после** того, как переменные окружения доступны.

3. **Попробуйте перезапустить деплой:**
   - В Railway → ваш сервис → **"Deployments"**
   - Нажмите **"Redeploy"**

## Важно

- `DATABASE_URL` должен быть добавлен **до** запуска `npx prisma generate`
- Railway автоматически делает переменные доступными во время сборки
- Если используете Service Reference, Railway автоматически обновит URL при изменении PostgreSQL


